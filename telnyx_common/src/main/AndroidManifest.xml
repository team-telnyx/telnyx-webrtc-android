<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Background service for handling call decline without launching the main app -->
        <service
            android:name=".notification.BackgroundCallDeclineService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>